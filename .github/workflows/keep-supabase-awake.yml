name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 7 */3 * *" # every day at 07:00 UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://laremattjpwkgpmwitzv.supabase.co/rest/v1/
